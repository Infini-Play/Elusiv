import 'package:flutter/material.dart';

class LoginRegisterTextBox extends StatelessWidget {
  const LoginRegisterTextBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}